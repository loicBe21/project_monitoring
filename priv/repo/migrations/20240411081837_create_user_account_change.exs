defmodule PmLogin.Repo.Migrations.CreateUserAccountChange do
  use Ecto.Migration

  def change do
#
 #   create table(:user_account_change) do
  #    add :user_id, references("users")
  #    add :changed_by_user_id, references("users")
 #     add :change_details , :string , null: false
 #     add :changed_at , :naive_datetime
  #    timestamps()
   # end

  end
end
